syntax = "proto3";
package claude.v1;

message RunRequest {
  string prompt = 1;
}

message RunResponse {
  string output = 1;
}

service ClaudeService {
  rpc Run(RunRequest) returns (RunResponse) {}
}
