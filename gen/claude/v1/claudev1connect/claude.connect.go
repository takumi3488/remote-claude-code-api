// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: claude/v1/claude.proto

package claudev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	v1 "remote-claude-code-api/gen/claude/v1"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ClaudeServiceName is the fully-qualified name of the ClaudeService service.
	ClaudeServiceName = "claude.v1.ClaudeService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ClaudeServiceRunProcedure is the fully-qualified name of the ClaudeService's Run RPC.
	ClaudeServiceRunProcedure = "/claude.v1.ClaudeService/Run"
)

// ClaudeServiceClient is a client for the claude.v1.ClaudeService service.
type ClaudeServiceClient interface {
	Run(context.Context, *v1.RunRequest) (*v1.RunResponse, error)
}

// NewClaudeServiceClient constructs a client for the claude.v1.ClaudeService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewClaudeServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ClaudeServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	claudeServiceMethods := v1.File_claude_v1_claude_proto.Services().ByName("ClaudeService").Methods()
	return &claudeServiceClient{
		run: connect.NewClient[v1.RunRequest, v1.RunResponse](
			httpClient,
			baseURL+ClaudeServiceRunProcedure,
			connect.WithSchema(claudeServiceMethods.ByName("Run")),
			connect.WithClientOptions(opts...),
		),
	}
}

// claudeServiceClient implements ClaudeServiceClient.
type claudeServiceClient struct {
	run *connect.Client[v1.RunRequest, v1.RunResponse]
}

// Run calls claude.v1.ClaudeService.Run.
func (c *claudeServiceClient) Run(ctx context.Context, req *v1.RunRequest) (*v1.RunResponse, error) {
	response, err := c.run.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// ClaudeServiceHandler is an implementation of the claude.v1.ClaudeService service.
type ClaudeServiceHandler interface {
	Run(context.Context, *v1.RunRequest) (*v1.RunResponse, error)
}

// NewClaudeServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewClaudeServiceHandler(svc ClaudeServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	claudeServiceMethods := v1.File_claude_v1_claude_proto.Services().ByName("ClaudeService").Methods()
	claudeServiceRunHandler := connect.NewUnaryHandlerSimple(
		ClaudeServiceRunProcedure,
		svc.Run,
		connect.WithSchema(claudeServiceMethods.ByName("Run")),
		connect.WithHandlerOptions(opts...),
	)
	return "/claude.v1.ClaudeService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ClaudeServiceRunProcedure:
			claudeServiceRunHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedClaudeServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedClaudeServiceHandler struct{}

func (UnimplementedClaudeServiceHandler) Run(context.Context, *v1.RunRequest) (*v1.RunResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("claude.v1.ClaudeService.Run is not implemented"))
}
